/**
 * Logowanie, rejestracja i wejście jako gość.
 * Do napisania: formularz, wywołania z api/client.ts, zapis tokenu przez session.save.
 */
export function LoginPage() {
  return <p>Tu będzie logowanie.</p>;
}
